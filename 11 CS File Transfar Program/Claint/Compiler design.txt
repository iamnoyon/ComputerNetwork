Parsing is a process of analyzing a text, made of sequence of tokens, to determine its grammatical structure with
 respect to a given grammar.The parser obtains a string of token from the lexical analyzer and verifies that the string
 can be generated by the grammar for the source language.it is expected  ,the parser to report syntactical error and
 sometime recover from commonly occurring errors.

EOF